<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>
        <title>I Was a Sole Survivor, a Solitary Buyer</title>
    </head>
    <body>
        <div class="container">
            <div class="jumbotron">
                <h1><a href="/">Shoes</a></h1>
                <h4>The Finest Treads and Their Purveyors</h4>
            </div>
            <ul class="list-group">
                <li class="list-group-item active"><h4>Stores (Or Should They Be Boot-iques?)</h4></li>
                {% if stores is not empty %}
                    {% for store in stores %}
                        <li class="list-group-item"><a href="/stores/{{ store.getId }}"><h4>{{ store.name }} (Brands Carried: {{ store.getBrands|length }})</h4></a></li>
                    {% endfor %}
                {% else %}
                    <li class="list-group-item"><h4>No stores found! Wanna add one?</h4></li>
                {% endif %}
            </ul>
            <hr>
            <h3>Add a Store</h3>
            <form action="/stores/add_store" method="post">
                <div class="form-group">
                    <label for="store_name">Store Name</label>
                    <input type="text" class="form-control" name="store_name" id="store_name" required>
                </div>
                <button type="submit" class="btn btn-success">Add a Store</button>
                <button type="button" class="btn btn-warning btn-clear-fields">Clear Name</button>
            </form>
            <hr>
            <h3>Remove All Stores</h3>
            <form action="/stores/delete_all" method="post">
                <button type="submit" class="btn btn-danger">Do It</button>
                <a href="/" class="btn btn-warning btn-clear-fields">Or Don't</a>
            </form>
        </div>
    </body>
</html>
